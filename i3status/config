general {
  colors = true
  interval = 5
}

order += "battery all"
order += "tztime date"
order += "tztime time"

tztime date {
  format = "%a %d %b"
}

tztime time {
  format = "%H:%M"
}

battery all {
  format = "%percentage"
}

