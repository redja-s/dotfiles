# Starship CLI configuration

Configuration for starship (https://starship.rs/config/)

## Setup

Same as other `.config` files

1. Symlink into `.config`: `sudo ln -s $(pwd)/<path_to_file>/starship.toml ~/.config/`
